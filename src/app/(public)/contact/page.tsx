import Link from "next/link";
import { ArrowLeft, ArrowUpRight, Mail, MapPin } from "lucide-react";
import { profile } from "@/data/site";
import { ContactForm } from "@/components/contact/contact-form";
export default function ContactPage() { return <main className="content-page contact-page"><div className="wrap content-hero"><Link className="back-link" href="/"><ArrowLeft size={16} /> Home</Link><p className="section-kicker">09 / CONTACT</p><h1>Let&apos;s make<br /><em>something useful.</em></h1><p className="content-lead">Have a dashboard to clarify, a system to build, a research problem to untangle, or a team to train?</p></div><div className="wrap contact-grid"><div className="contact-note"><p>Tell me where the friction is. I&apos;ll help you find the signal, shape the system, or teach the team.</p><span><MapPin size={16} /> Nairobi, Kenya</span><span><Mail size={16} /> <a href={`mailto:${profile.email}`}>{profile.email}</a></span><span><ArrowUpRight size={16} /> <a href={profile.linkedin} target="_blank" rel="noreferrer">LinkedIn</a></span><span><ArrowUpRight size={16} /> <a href={profile.github} target="_blank" rel="noreferrer">GitHub</a></span></div><ContactForm /></div></main>; }
