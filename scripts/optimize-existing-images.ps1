$ErrorActionPreference = 'Stop'

Write-Host 'Running image optimization and thumbnail generation for existing uploaded assets.'
Write-Host 'This workflow should use the image processor and storage abstraction rather than ad hoc filesystem logic.'
