"use client";
import { useState } from "react";
import { ArrowUpRight, Eye, EyeOff, LoaderCircle } from "lucide-react";
import { requestPasswordReset, resetPassword } from "@/server/actions/auth";
import styles from "./password-field.module.css";
export function ForgotPasswordForm() { const [email, setEmail] = useState(""); const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false); return <form className="login-form" onSubmit={async (event) => { event.preventDefault(); setBusy(true); const result = await requestPasswordReset({ email }); setMessage(result.message); setBusy(false); }}><label>Email address<input required type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="you@example.com" /></label><button className="button button-dark" disabled={busy}>{busy ? <LoaderCircle className="spin" size={16} /> : <ArrowUpRight size={16} />} Send reset link</button>{message && <p className="account-message">{message}</p>}</form>; }
export function ResetPasswordForm({ token }: { token: string }) { const [password, setPassword] = useState(""); const [showPassword, setShowPassword] = useState(false); const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false); return <form className="login-form" onSubmit={async (event) => { event.preventDefault(); setBusy(true); const result = await resetPassword({ token, password }); setMessage(result.message); setBusy(false); }}><label className={styles.field}>New password<input required minLength={8} type={showPassword ? "text" : "password"} value={password} onChange={(event) => setPassword(event.target.value)} placeholder="At least 8 characters" /><button className={styles.toggle} type="button" onClick={() => setShowPassword(!showPassword)} aria-label={showPassword ? "Hide password" : "Show password"}>{showPassword ? <EyeOff size={18} /> : <Eye size={18} />}</button></label><button className="button button-dark" disabled={busy}>{busy ? <LoaderCircle className="spin" size={16} /> : <ArrowUpRight size={16} />} Set new password</button>{message && <p className="account-message">{message}</p>}</form>; }
