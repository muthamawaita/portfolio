$ErrorActionPreference = 'Stop'

Write-Host 'Seeding default roles, permissions, plans, and template metadata.'
Write-Host 'This script should run through the backend seed routine and should not include production secrets.'
