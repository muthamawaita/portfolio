import Link from "next/link";
import { ArrowLeft, Download, FileSpreadsheet, NotebookTabs } from "lucide-react";

const resources = [{ type: "NOTEBOOK", title: "Customer Churn Analysis", detail: "A guided Python notebook from cleaning to model interpretation.", icon: NotebookTabs }, { type: "DATASET", title: "Synthetic Operations Dataset", detail: "A privacy-safe dataset for practising KPI and backlog analysis.", icon: FileSpreadsheet }, { type: "TEACHING KIT", title: "Dashboard Assessment Rubric", detail: "A practical rubric for evaluating analytical storytelling and usability.", icon: Download }];
export default function ResourcesPage() { return <main className="content-page"><div className="wrap content-hero"><Link className="back-link" href="/"><ArrowLeft size={16} /> Home</Link><p className="section-kicker">06 / RESOURCES</p><h1>Useful things<br /><em>to take with you.</em></h1><p className="content-lead">Open notebooks, synthetic datasets, and practical teaching materials for learning by doing.</p></div><div className="wrap resource-grid">{resources.map(({ type, title, detail, icon: Icon }) => <article className="resource-card" key={title}><div className="resource-icon"><Icon size={23} /></div><p className="section-kicker">{type}</p><h2>{title}</h2><p>{detail}</p><button className="text-link"><Download size={15} /> Download resource</button></article>)}</div></main>; }
